name: Release

on:
  release:
    types: [created]

jobs:
  build-dmfsi:
    name: Build DMFSI Module
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    container:
      image: chocotechnologies/dmod:1.0.4
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Extract version from tag
        id: get_version
        run: |
          # Extract version from tag (e.g., v1.2 -> 1.2)
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"  # Remove 'v' prefix if present
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"
      
      - name: Build DMFSI module
        run: |
          set -e
          mkdir -p build
          cd build
          
          cmake .. \
            -DDMOD_MODULE_VERSION="${{ steps.get_version.outputs.version }}"
          
          cmake --build .
          
          echo "Build completed"
          ls -la dmf/ || echo "No dmf directory"
          ls -la dmfc/ || echo "No dmfc directory"
      
      - name: Prepare DMFSI release package
        run: |
          set -e
          mkdir -p release_package
          BUILD_DIR=build
          DMF_DIR="$BUILD_DIR/dmf"
          DMFC_DIR="$BUILD_DIR/dmfc"
          
          # Copy DMF files
          cp "$DMF_DIR/dmfsi.dmf" release_package/
          cp "$DMF_DIR/dmfsi_version.txt" release_package/
          
          # Copy DMFC if exists
          if [ -f "$DMFC_DIR/dmfsi.dmfc" ]; then
            cp "$DMFC_DIR/dmfsi.dmfc" release_package/
          fi
          
          # Copy DMD if exists
          if [ -f "$DMF_DIR/dmfsi.dmd" ]; then
            cp "$DMF_DIR/dmfsi.dmd" release_package/
          fi
          
          # Copy header files
          mkdir -p release_package/inc
          cp inc/dmfsi.h release_package/inc/
          if [ -f "$BUILD_DIR/dmfsi_defs.h" ]; then
            cp "$BUILD_DIR/dmfsi_defs.h" release_package/inc/
          fi
          
          # Copy documentation
          mkdir -p release_package/docs
          cp -r docs/* release_package/docs/
          
          # Copy DMR resource file
          cp dmfsi.dmr release_package/
          
          # Copy README, LICENSE, and IMPLEMENTATION
          cp README.md release_package/
          cp LICENSE release_package/
          cp IMPLEMENTATION.md release_package/
          
          # Create release notes file from GitHub release
          echo "${{ github.event.release.body }}" > release_package/RELEASE_NOTES.txt
          
          echo "Package contents:"
          ls -la release_package/
      
      - name: Create DMFSI release archive
        run: |
          cd release_package
          zip -r ../dmfsi-${{ github.event.release.tag_name }}.zip .
          cd ..
          echo "Created archive:"
          ls -lh dmfsi-*.zip
      
      - name: Upload DMFSI artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-dmfsi
          path: dmfsi-${{ github.event.release.tag_name }}.zip
          retention-days: 1

  build-ramfs:
    name: Build RamFS Example
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    container:
      image: chocotechnologies/dmod:1.0.4
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Extract version from tag
        id: get_version
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"
      
      - name: Build DMFSI and RamFS
        run: |
          set -e
          mkdir -p build
          cd build
          
          cmake .. \
            -DDMOD_MODULE_VERSION="${{ steps.get_version.outputs.version }}"
          
          cmake --build .
          
          echo "Build completed"
          ls -la dmf/ || echo "No dmf directory"
      
      - name: Prepare RamFS release package
        run: |
          set -e
          mkdir -p release_package_ramfs
          BUILD_DIR=build
          DMF_DIR="$BUILD_DIR/dmf"
          DMFC_DIR="$BUILD_DIR/dmfc"
          
          # Copy RamFS DMF file
          cp "$DMF_DIR/ramfs.dmf" release_package_ramfs/
          cp "$DMF_DIR/ramfs_version.txt" release_package_ramfs/
          
          # Copy DMFC if exists
          if [ -f "$DMFC_DIR/ramfs.dmfc" ]; then
            cp "$DMFC_DIR/ramfs.dmfc" release_package_ramfs/
          fi
          
          # Copy DMD if exists
          if [ -f "$DMF_DIR/ramfs.dmd" ]; then
            cp "$DMF_DIR/ramfs.dmd" release_package_ramfs/
          fi
          
          # Copy source and build files
          mkdir -p release_package_ramfs/src
          cp examples/ramfs/ramfs.c release_package_ramfs/src/
          cp examples/ramfs/Makefile release_package_ramfs/
          cp examples/ramfs/CMakeLists.txt release_package_ramfs/
          
          # Copy DMR resource file
          cp examples/ramfs/ramfs.dmr release_package_ramfs/
          
          # Copy README
          cp examples/ramfs/README.md release_package_ramfs/
          
          echo "Package contents:"
          ls -la release_package_ramfs/
      
      - name: Create RamFS release archive
        run: |
          cd release_package_ramfs
          zip -r ../ramfs-${{ github.event.release.tag_name }}.zip .
          cd ..
          echo "Created archive:"
          ls -lh ramfs-*.zip
      
      - name: Upload RamFS artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-ramfs
          path: ramfs-${{ github.event.release.tag_name }}.zip
          retention-days: 1

  generate-versions-manifest:
    name: Generate versions.dmm
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history to get all tags
      
      - name: Generate versions.dmm
        run: |
          set -e
          echo "# List of available versions for dmfsi modules" > versions.dmm
          echo "# Generated automatically by CI" >> versions.dmm
          echo "" >> versions.dmm
          
          # Get all version tags (starting with 'v') and extract version numbers
          VERSIONS=$(git tag -l 'v*' | sed 's/^v//' | sort -V | tr '\n' ' ' | sed 's/ $//')
          
          # Remove vlatest from the list if present
          VERSIONS=$(echo $VERSIONS | sed 's/\blatest\b//g' | xargs)

          if [ -z "$VERSIONS" ]; then
            echo "Warning: No version tags found"
            VERSIONS="${{ github.event.release.tag_name }}"
            VERSIONS="${VERSIONS#v}"
          fi
          
          echo "Found versions: $VERSIONS"
          
          # Add $version-available directives for the modules
          echo "\$version-available dmfsi $VERSIONS" >> versions.dmm
          echo "\$version-available ramfs $VERSIONS" >> versions.dmm
          
          echo "Generated versions.dmm:"
          cat versions.dmm
      
      - name: Upload versions.dmm as artifact
        uses: actions/upload-artifact@v4
        with:
          name: versions-manifest
          path: versions.dmm
          retention-days: 1

  upload-release-assets:
    name: Upload Release Assets
    needs: [build-dmfsi, build-ramfs, generate-versions-manifest]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Display artifact structure
        run: |
          echo "Downloaded artifacts:"
          ls -lR artifacts/
      
      - name: Upload release assets to versioned tag
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          # Upload all release archives to the GitHub release
          shopt -s nullglob
          zip_files=(artifacts/release-*/*.zip)
          
          if [ ${#zip_files[@]} -eq 0 ]; then
            echo "Error: No artifacts found to upload"
            exit 1
          fi
          
          for zip_file in "${zip_files[@]}"; do
            echo "Uploading $zip_file to ${{ github.event.release.tag_name }}..."
            gh release upload ${{ github.event.release.tag_name }} \
              "$zip_file" \
              --repo ${{ github.repository }} \
              --clobber
          done
          
          # Upload versions.dmm to the versioned release
          if [ -f artifacts/versions-manifest/versions.dmm ]; then
            echo "Uploading versions.dmm to ${{ github.event.release.tag_name }}..."
            gh release upload ${{ github.event.release.tag_name }} \
              artifacts/versions-manifest/versions.dmm \
              --repo ${{ github.repository }} \
              --clobber
          fi
          
          echo "Successfully uploaded ${#zip_files[@]} artifact(s) to ${{ github.event.release.tag_name }}"
      
      - name: Create or update latest release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          
          # Check if vlatest release exists
          if gh release view vlatest --repo ${{ github.repository }} >/dev/null 2>&1; then
            echo "Release vlatest exists, deleting it..."
            gh release delete vlatest --repo ${{ github.repository }} --yes
          fi
          
          # Create new vlatest release
          echo "Creating vlatest release..."
          gh release create vlatest \
            --repo ${{ github.repository }} \
            --title "Latest Release (based on ${{ github.event.release.tag_name }})" \
            --notes "This release always points to the latest stable version. Currently based on ${{ github.event.release.tag_name }}."
      
      - name: Upload release assets to latest tag
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          shopt -s nullglob
          zip_files=(artifacts/release-*/*.zip)
          
          for zip_file in "${zip_files[@]}"; do
            echo "Uploading $zip_file to vlatest..."
            gh release upload vlatest \
              "$zip_file" \
              --repo ${{ github.repository }} \
              --clobber
          done
          
          # Upload versions.dmm to the latest release
          if [ -f artifacts/versions-manifest/versions.dmm ]; then
            echo "Uploading versions.dmm to vlatest..."
            gh release upload vlatest \
              artifacts/versions-manifest/versions.dmm \
              --repo ${{ github.repository }} \
              --clobber
          fi
          
          echo "Successfully uploaded ${#zip_files[@]} artifact(s) to vlatest"
